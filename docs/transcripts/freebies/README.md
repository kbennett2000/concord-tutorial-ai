# Cook receipts (T6)

One real cook per recipe, on the real lesson pages, using the recipe's
shipped wording. Two notes for the auditor:

- **`r3-translation-broken-first-cook/`** is kept on purpose: the first
  R3 wording changed only the two ask-lines, the formatter read a KJV
  key that wasn't there, and the model got an empty tool result. The
  shipped recipe names all four edits and tells this story. Untouched
  evidence of a recipe the cook caught.
- **`r6-lan/session.json`** has this machine's LAN address replaced
  with `<your-concord-host>` (family rule: placeholder hosts only in
  the repo). That hostname substitution is the only edit ever made to
  a committed transcript, it is mandated by ruling 6, and it is
  declared here.
- `r1b-excerpt/` exercises R1's excerpt branch specifically (Esther
  8:9, Scripture's longest verse — the "…" signal and the `[excerpt]`
  marker on the wire).
